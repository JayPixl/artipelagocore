# Single root glossary for AI development

We keep one root `CONTEXT.md` with grouped sections instead of per-module contexts, because the modules share the event-effect vocabulary and the repo is small enough that one glossary stays unambiguous.
